import express from 'express';
import { getAbout, createAbout } from '../controllers/aboutController';

const router = express.Router();

router.get('/', getAbout);
router.post('/', createAbout);

export default router;
